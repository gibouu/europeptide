# Tasks
One H2 section per task.

## Deploy site to GitHub Pages
status: IN_PROGRESS
opened: 2026-06-12
steps:
  - [ ] vite base + router basename + SPA 404 fallback
  - [ ] Pages workflow (.github/workflows/pages.yml)
  - [ ] enable Pages, push, verify live URL
notes:
  - checkout needs the payment server; Pages build is static demo (test mode only)
